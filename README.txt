Roberto Garza
xaleth09

all programs made with: make

matmult_p:
only limitations are the length of each number in each row
can be at max LNGEST_NUM ( = 10) which is defined at the top of the file
as well and LNGEST_ROWSIZE ( = 50) which is also defined at the top of the file
and both can be incremented if necessary

matmult_t:
only reads in matrices, and does error checking
implements context but segfaults 
